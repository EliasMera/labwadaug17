1. Si ya tiene proyecto no debe salir el tab de registrar proyecto
2. En editar proyecto debe aparecer la informacion que ya se habia puesto, y semestre no debe de estar ya que se agarra esa informacion del semestre actual (ahorita no hay de donde agarrar ese dato) el formato es AD-17 o EM-18
3. En registro de proyecto, la descripción debe de ser a lo mas de 600 caracteres (validarlo), la informacion de alumnos debe contener todos los campos para registrarlo (excepto el grupo ya que se entiende, su contraseña ya debe de estar encriptada (matricula))
4. Clasificacion sectorial debe ser un drop down que agarre el contenido que esta en la base de datos
5. Número de integrantes en el registro puede ser campo de texto para asi mostrar los campos de texto de alumnos necesarios
6. Agregar un editar mi información (alumno logeado)
7. Hacer logout
8. Debo ver la tabla de comentarios
9. cambiar el check session con lo que tiene ivan en su archivo de application layer.
10. En el historial de proyectos solo deben salir los proyectos que tengan active = false, al crear un proyecto el active por default debe ser 1, eso se debe ajustar directamente en la base de datos no se manda en el SQL. Esto significa que solo se debe mostrar los proyectos de semestres pasados.
11. En subir archivos se deben traer los camposa subir que esten activos en required files que hace el admin.

'''''''NUEVO'''''''''

* Correr este script en la base de datos ALTER TABLE `projects` ADD `participant` BOOLEAN NOT NULL DEFAULT FALSE, esto solo agrega una columna. 

* Si participante es true significa que voy a entrar a la muestra entonces se va tener que subir el CD, debe aparecer una pestaña "Subir documentos" al entrar solo habrá una ventana con 4 letreros (Documento Word/PDF, presentación ppt, poster (jpeg, o cualquier formto de imagen) y comprobante de pago (jpeg, o cualquier formato de imagen) esto se debe ser guardado en el servidor. En files se debe guardar la ruta y la fecha en que se subio.


------ Set teacher ID unique en el PHPmydmin por mientras, ya que despues Ivan Leal va ponerlo dentro del script